import {
  forwardRef,
  type ForwardRefRenderFunction
} from 'react';

import {
  clsx
} from 'clsx';

import Props   from './component.props';
import classes from './component.module.scss';

const Pagination: ForwardRefRenderFunction<HTMLDivElement, Props> = (props, ref) => {

  const {
    children,
    className,
    ...otherProps
  } = props;

  return (
    <nav ref={ref} className={clsx(className, classes['pagination'])} {...otherProps}>
      {children}
    </nav>
  );

};

export default forwardRef(Pagination);